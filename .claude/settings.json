{
  "permissions": {
    "allow": [
      "Bash(gh pr view:*)",
      "Bash(gh pr diff:*)",
      "Bash(pnpm test)",
      "Bash(pnpm typecheck)",
      "Bash(pnpm lint)",
      "Bash(pnpm check)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "echo \"$CLAUDE_TOOL_INPUT\" | python3 -c \"import sys,json; d=json.load(sys.stdin); path=d.get('file_path',''); exit(2 if 'pnpm-lock.yaml' in path else 0)\"",
            "statusMessage": "lock ファイルの編集をチェック中..."
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "pnpm check && pnpm typecheck && pnpm test 2>/dev/null || true",
            "statusMessage": "format / typecheck / test 実行中...",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
